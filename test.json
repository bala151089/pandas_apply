// def summarize_wind(df):
//     total_count = len(df)

//     summary = (
//         df.groupby("wind")
//           .agg(
//               count=("wind", "count"),
//               xyz_sum=("xyz", "sum")
//           )
//           .reset_index()
//     )

//     # Calculate percentage
//     summary["percentage"] = (summary["count"] / total_count * 100).round(0).astype(int).astype(str) + "%"

//     # Reorder columns
//     summary = summary[["wind", "count", "percentage", "xyz_sum"]]

//     return summary