

{
    "logic_blocks": [
        {
            "block_type": "IF",
            "condition_operator": "AND",
            "rules": [
                {"variable": "abc","operator":">","value":"123",
"segment": "wind_1"
                },
                {"variable": "xyz","operator": ">","value": "123",
"segment": "wind_2"
                }
            ]
        },
         {
            "block_type": "IF",
            "condition_operator": "OR",
            "rules": [
                {"variable": "abc","operator":"<","value":"123",
"segment": "wind_1"
                },
                {"variable": "xyz","operator": "<","value": "123",
"segment": "wind_2"
                }
            ]
        },
        {
            "block_type": "ELSE",
            "segment": "wind_2"
            
        }
    ]
}
// import pandas as pd
// import numpy as np
// import operator

// def apply_logic_blocks(df: pd.DataFrame, logic_config: dict, output_col="segment"):
//     # Mapping operators
//     ops = {
//         ">": operator.gt,
//         "<": operator.lt,
//         ">=": operator.ge,
//         "<=": operator.le,
//         "==": operator.eq,
//         "!=": operator.ne
//     }

//     # Initialize output column
//     df[output_col] = None

//     # Track rows already assigned (for IF → ELIF behavior)
//     assigned_mask = pd.Series(False, index=df.index)

//     for block in logic_config["logic_blocks"]:
//         block_type = block["block_type"]

//         # ELSE block
//         if block_type == "ELSE":
//             df.loc[~assigned_mask, output_col] = block["segment"]
//             continue

//         rules = block["rules"]
//         condition_operator = block.get("condition_operator", "AND")

//         masks = []

//         for rule in rules:
//             col = rule["variable"]
//             op = rule["operator"]
//             val = rule["value"]

//             # Convert value type if needed
//             try:
//                 val = float(val)
//                 series = pd.to_numeric(df[col], errors='coerce')
//             except:
//                 series = df[col]

//             mask = ops[op](series, val)
//             masks.append(mask)

//         # Combine masks
//         if condition_operator == "AND":
//             final_mask = masks[0]
//             for m in masks[1:]:
//                 final_mask &= m
//         elif condition_operator == "OR":
//             final_mask = masks[0]
//             for m in masks[1:]:
//                 final_mask |= m
//         else:
//             raise ValueError(f"Unsupported condition_operator: {condition_operator}")

//         # Apply only to unassigned rows (IF → ELIF behavior)
//         final_mask = final_mask & (~assigned_mask)

//         # Assign segment (take first rule's segment or customize)
//         segment_value = rules[0].get("segment")

//         df.loc[final_mask, output_col] = segment_value

//         # Update assigned mask
//         assigned_mask |= final_mask

//     return df